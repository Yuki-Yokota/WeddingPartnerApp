<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.weddingpartnerapp.repository.UserMapper">
	<resultMap id="userResultMap" type="User">
		<id property = "userId" column="USER_ID"/>
		<result property = "userName" column="USER_NAME"/>
		<result property = "mailAddress" column="MAIL_ADDRESS"/>
		<result property = "role" column="role"/>
		<result property = "password" column="password"/>
		<result property = "salt" column="salt"/>
	</resultMap>
	
	<select id="findAllUser" resultMap="userResultMap">
		select * from users
	</select>
	<select id="findUserByMailAddress" resultMap="userResultMap">
		select * from users WHERE mail_address = #{mailAddress}
	</select>
	<select id="findById" resultMap="userResultMap">
		select * from users WHERE user_id = #{userId}
	</select>
	<select id="count" resultType="int">
       SELECT MAX(USER_ID) FROM users
   </select>
   <select id="countByMailAddress" resultType="int">
       SELECT MAX(USER_ID) FROM users WHERE mail_address = #{mailAddress}
   </select>
	<insert id="insert" parameterType="User">
       INSERT INTO users
       (
           USER_ID,
           USER_NAME,
           MAIL_ADDRESS,
           ROLE,
           PASSWORD,
       	SALT
       )
       VALUES
       (
           #{userId},
           #{userName},
           #{mailAddress},
           #{role},
           #{password},
           #{salt}
       )
   </insert>
   <update id="update" parameterType="User">
       UPDATE users
       SET
           user_name = #{userName},
           mail_address = #{mailAddress},
           password = #{password},
           salt = #{salt}
       WHERE
           user_id = #{userId}
   </update>
   <delete id="delete" parameterType="User">
       DELETE FROM
           users
       WHERE
           user_id = #{userId}
   </delete>
</mapper>
